# Icons

Placeholder directory. Drop `icon-16.png`, `icon-32.png`, `icon-48.png`, and `icon-128.png` here before shipping.

Chrome will warn on load without these but will still run the extension during local development.
